# Leave blank for local dev (Vite proxy handles it — see vite.config.js).
# For production, set this to your deployed backend's URL, e.g.:
# VITE_API_BASE_URL=https://llm-guard-chat-backend.onrender.com/api
VITE_API_BASE_URL=
